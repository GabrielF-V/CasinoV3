spring:
  application:
    name: api-gateway
  config:
    import: ${SPRING_CONFIG_IMPORT:configserver:http://localhost:8888}
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true